const powerFailure = (powerFail) => {
  const economy = {
    economyFresh: false,
    economyCool: false,
    economyHeat: false,
  }
}

describe('arry', () => {
  it('returns empty economy if powerFail is off', () => {})
})
